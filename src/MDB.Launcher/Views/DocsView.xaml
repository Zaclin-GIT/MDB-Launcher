<UserControl x:Class="MDB.Launcher.Views.DocsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:vm="clr-namespace:MDB.Launcher.ViewModels"
             xmlns:conv="clr-namespace:MDB.Launcher.Converters"
             mc:Ignorable="d"
             d:DataContext="{d:DesignInstance vm:DocsViewModel}"
             d:DesignWidth="800" d:DesignHeight="600">
    <UserControl.Resources>
        <conv:BoolToVisibilityConverter x:Key="BoolToVis" />
    </UserControl.Resources>

    <Grid>
        <!-- Loading overlay -->
        <StackPanel VerticalAlignment="Center"
                    HorizontalAlignment="Center"
                    Visibility="{Binding IsLoading, Converter={StaticResource BoolToVis}}">
            <ProgressBar Style="{StaticResource MdbProgressBar}"
                         IsIndeterminate="True"
                         Width="200"
                         Margin="0,0,0,12" />
            <TextBlock Style="{StaticResource Body}"
                       Text="Loading documentation..."
                       HorizontalAlignment="Center"
                       Foreground="{StaticResource TextMutedBrush}" />
        </StackPanel>

        <!-- WebBrowser -->
        <WebBrowser x:Name="DocsBrowser"
                    Navigating="DocsBrowser_Navigating"
                    LoadCompleted="DocsBrowser_LoadCompleted" />
    </Grid>
</UserControl>
